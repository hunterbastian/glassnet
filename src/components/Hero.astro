---
// Hero component with glass panel and floating bubbles
---

<section class="relative h-screen flex items-center justify-center px-6 fade-in-section">
  <!-- Reduced hero bubbles for better performance -->
  <div class="absolute inset-0 pointer-events-none overflow-hidden">
    <div class="bubble w-16 h-16 top-1/4 left-1/4 opacity-60" style="animation-delay: 1s;"></div>
    <div class="bubble bubble-slow w-14 h-14 top-2/3 right-1/4 opacity-60" style="animation-delay: 4s;"></div>
  </div>

  <!-- Glass panel container -->
  <div class="glass-strong max-w-4xl w-full mx-auto p-8 md:p-12 text-center lens-flare relative z-10">
    <!-- Main title with gradient text -->
    <h1 class="text-5xl sm:text-6xl lg:text-7xl xl:text-8xl font-bold mb-4 md:mb-6 text-transparent bg-clip-text bg-gradient-to-r from-white via-blue-100 to-purple-200 leading-tight">
      GlassNet
    </h1>
    
    <!-- Subheading -->
    <p class="text-lg sm:text-xl lg:text-2xl text-white/90 mb-6 md:mb-8 max-w-2xl mx-auto leading-relaxed">
      Experience the future of digital aesthetics with our stunning Frutiger Aero-inspired design language
    </p>
    
    <!-- Decorative elements -->
    <div class="absolute -top-4 -right-4 w-8 h-8 rounded-full bg-gradient-to-br from-white/60 to-blue-300/60 blur-sm"></div>
    <div class="absolute -bottom-6 -left-6 w-12 h-12 rounded-full bg-gradient-to-br from-purple-300/40 to-pink-300/40 blur-sm"></div>
  </div>
</section>

<style>
  /* Additional hero-specific animations */
  @keyframes pulse {
    0%, 100% { opacity: 0.6; }
    50% { opacity: 1; }
  }
  
  .lens-flare::after {
    content: '';
    position: absolute;
    top: 20%;
    right: 20%;
    width: 60px;
    height: 60px;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.3) 0%, transparent 70%);
    border-radius: 50%;
    filter: blur(10px);
    animation: pulse 6s ease-in-out infinite;
    pointer-events: none;
  }
</style>
